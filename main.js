function delimit(){
const paréntesis ")" , "("
las llaves {}
corchetes []
}

let expresion = (prompt){
    "ingrese la expresion que requiera"
    
};{
function delimit(expresion)
    if (delimit == ("{}"))
        console.log ("True")
    else
        console.log ("False")

}
    if (delimit == ("()"))
        console.log ("True")
    else
        console.log ("False"){

};
    if (delimit == ("[]"))
        console.log ("True")
    else
        console.log ("False")

function equi (expresion) {
    if expresion !==
}

function exep(){
    if (expresion.length >1)
        console.log ("caracteres insuficientes para hacerm algo con ellos")
    if (expresion.arguments !== ("(",")","[","]","{","}"))
        
}